Concept:
{{concept}}

{{#if instructions}}
Adjustment request:
{{instructions}}
{{/if}}

{{#if sceneDesign}}
Current plan to revise:
{{sceneDesign}}
{{/if}}

Extra requirements:
- Each card should describe concrete visual objects and actions.
- Each card should connect naturally to the previous one and state the transition.
- Use visible changes like appearing, moving, splitting, gathering, projecting, morphing, contrasting, and highlighting instead of abstract commentary.

Generate the next problem-framing card now.
